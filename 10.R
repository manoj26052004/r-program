# Define a vector
my_vector <- c(12, 45, 6, 23, 9, 15, 30, 7, 42)

# Find the maximum and minimum values
max_value <- max(my_vector)
min_value <- min(my_vector)

# Print the results
cat("Maximum value:", max_value, "\n")
cat("Minimum value:", min_value, "\n")